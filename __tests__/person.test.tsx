import Person from "@/components/person";
import { render, screen } from "@testing-library/react";
import { mockedUser } from "../__mocks__/mocks";

describe("Person", () => {
  it("renders a component", () => {
    // render(<Person handleNextPerson={() => null} data={mockedUser} />);
  });
});
